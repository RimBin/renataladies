Dev note: runs `web/` app on port 3005

How to start:
- From repository root run: npm run dev

Notes:
- The root is a monorepo wrapper; the real Next.js app is in `web/` and it sets --port 3005.
- See `web/PORT.md` for the explicit port and commands.

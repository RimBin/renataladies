Dev port: 2005

Notes:
- `package.json` scripts use `--port 2005` for `dev`, `dev:clean`, `dev:turbo` and `start`.
- Start with: npm run dev (from web/ or from repo root using the wrapper script: npm run dev)
